diff --git a/src/Views/Components/info.xml.twig b/src/Views/Components/info.xml.twig
--- a/src/Views/Components/info.xml.twig
+++ b/src/Views/Components/info.xml.twig
@@ -2,9 +2,15 @@
 <Clave>{{ document_key }}</Clave>
 <ProveedorSistemas>{{ issuer.identification_number }}</ProveedorSistemas>
 <CodigoActividadEmisor>{{ issuer.activity.code }}</CodigoActividadEmisor>
-{% if receiver.activity.code %}
+{# FEC (08): el XSD exige el nodo CodigoActividadReceptor en esta posición (antes de NumeroConsecutivo); no se puede omitir. Debe ser CIIU válido (minLength; no vacío). Venta: opcional si hay actividad. #}
+{% if document_type.code == '08' %}
+<CodigoActividadReceptor>{{ receiver.activity.code }}</CodigoActividadReceptor>
+{% else %}
+{% if receiver.activity.code %}
 <CodigoActividadReceptor>{{ receiver.activity.code }}</CodigoActividadReceptor>
 {% endif %}
+{% endif %}
 <NumeroConsecutivo>{{ sequential_number }}</NumeroConsecutivo>
 <FechaEmision>{{ date }}</FechaEmision>
 {% include 'Components/entity.xml.twig' with { 'entity': issuer, node: 'Emisor'
